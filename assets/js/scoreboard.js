class Scoreboard {

    AddUser(name, time) {
        var data = {
            name,
            time,
            date: new Date()
        };
    }

    FetchData() { }

    SaveData() { }

    DrawScoreboard() { }

    PlayersList() { }

    SaveScore(userName, score) { }
}
